@model IEnumerable<DemoMVC.Models.Person>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HoTen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QueQuan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoTen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QueQuan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CanCuocCongDan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CanCuocCongDan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CanCuocCongDan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
